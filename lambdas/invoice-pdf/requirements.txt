# Invoice PDF Lambda — Python dependencies

# PDF generation
# WeasyPrint 65.1 ships with compatible pydyf — no transform AttributeError
weasyprint==65.1

# HTML templating
Jinja2==3.1.4
MarkupSafe==2.1.5

# AWS SDK (provided by Lambda runtime, listed for local dev)
boto3==1.34.69
botocore==1.34.69
